# CHANGELOG

Contract changes only. Both people must agree before any entry is added here.
A "contract change" means a change to `db/schema.sql` or `openapi.json`.

Format: `[YYYY-MM-DD HH:MM UTC] [schema|api] [change description] — [initials of both approvers]`

---

## Frozen contracts

- `db/schema.sql` — frozen at initial commit
- `openapi.json` — frozen at initial commit (generated from FastAPI stubs)

---

<!-- entries go here -->
